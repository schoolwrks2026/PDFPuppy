<UserControl x:Class="PdfUtility.App.Views.ConvertView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700">
    <Grid>
        <Grid.RowDefinitions>
            <!-- Title -->
            <RowDefinition Height="Auto"/>
            <!-- Drag and Drop Dropzone Card -->
            <RowDefinition Height="*"/>
            <!-- Output settings card -->
            <RowDefinition Height="Auto"/>
            <!-- Action / Progress -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="🔄 Convert Files to PDF" Style="{StaticResource HeaderTitleStyle}"/>
            <TextBlock Text="Convert documents, spreadsheets, presentations, and images to high-quality PDF files offline." Style="{StaticResource SubtitleStyle}"/>
        </StackPanel>

        <!-- Drag & Drop Dropzone card -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="2" CornerRadius="10" Margin="0,0,0,16" Padding="24"
                AllowDrop="True" Drop="Border_Drop" DragOver="Border_DragOver">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="📥" FontSize="56" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <TextBlock Text="Drag and drop your file here" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="{StaticResource TextBrush}"/>
                    <TextBlock Text="or click below to browse files from your computer" FontSize="13" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" Margin="0,4,0,16"/>
                    <Button Content="Browse File" Command="{Binding BrowseFileCommand}" Style="{StaticResource ModernSecondaryButtonStyle}" HorizontalAlignment="Center" Click="BrowseFile_Click"/>

                    <!-- Selected File State -->
                    <Border Margin="0,16,0,0" Padding="12,8" Background="{StaticResource BackgroundBrush}" CornerRadius="6" Visibility="{Binding HasSelectedFile, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="📄" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding SelectedFileName}" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" MaxWidth="300" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Output Folder / Configuration Parameters card -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Save to: " FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBox Grid.Column="1" Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" VerticalAlignment="Center"/>
                <Button Grid.Column="2" Content="Browse Folder" Command="{Binding BrowseFolderCommand}" Style="{StaticResource ModernSecondaryButtonStyle}" VerticalAlignment="Center" Margin="8,0,0,0" Click="BrowseFolder_Click"/>
            </Grid>
        </Border>

        <!-- Actions panel (Convert, Cancel, and Progress feedback) -->
        <Border Grid.Row="3" Background="Transparent">
            <StackPanel>
                <Grid>
                    <!-- Progress Bar when converting -->
                    <ProgressBar Height="10" Minimum="0" Maximum="100" Value="{Binding ProgressValue}" Visibility="{Binding IsConverting, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,12"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Button Content="Convert to PDF" Command="{Binding ConvertCommand}" Style="{StaticResource ModernButtonStyle}" Width="160" Margin="0,0,8,0"/>
                        <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource ModernSecondaryButtonStyle}" Width="100" Visibility="{Binding IsConverting, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>

                    <!-- Status Label -->
                    <TextBlock Grid.Column="1" Text="{Binding ConversionStatus}" FontSize="13" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="16,0" TextTrimming="CharacterEllipsis"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
