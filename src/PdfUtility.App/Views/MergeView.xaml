<UserControl x:Class="PdfUtility.App.Views.MergeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700">
    <Grid>
        <Grid.RowDefinitions>
            <!-- Title Header -->
            <RowDefinition Height="Auto"/>
            <!-- PDF Merge List Grid -->
            <RowDefinition Height="*"/>
            <!-- Output Configuration (FileName and Directory) -->
            <RowDefinition Height="Auto"/>
            <!-- Actions Panel -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="🥞 Merge PDF Documents" Style="{StaticResource HeaderTitleStyle}"/>
            <TextBlock Text="Add two or more PDF files, arrange their order, and merge them into a single PDF document offline." Style="{StaticResource SubtitleStyle}"/>
        </StackPanel>

        <!-- File List Panel -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <!-- Toolbar Column -->
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <!-- Merge List Display Card -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="0" AllowDrop="True" Drop="FileList_Drop" DragOver="FileList_DragOver">
                <Grid>
                    <!-- Empty state placeholder message -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding HasFiles, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverted}">
                        <TextBlock Text="🥞" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock Text="Drag and drop PDF files here" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="{StaticResource TextBrush}"/>
                        <TextBlock Text="or click 'Add PDF Files' on the right to browse" FontSize="12" Foreground="{StaticResource MutedTextBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- PDF items ListBox -->
                    <ListBox x:Name="PdfListBox" ItemsSource="{Binding SelectedPdfFiles}" BorderThickness="0" Background="Transparent" Visibility="{Binding HasFiles, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="📄" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding FileName}" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" MaxWidth="300" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Grid.Column="2" Text="{Binding PageCount, StringFormat={}{0} Pages}" FontSize="12" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center" Margin="16,0"/>
                                    <TextBlock Grid.Column="3" Text="{Binding FileSizeFormatted}" FontSize="12" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Sidebar list action toolbar -->
            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                <Button Content="➕ Add PDF Files" Click="AddPdfFiles_Click" Style="{StaticResource ModernButtonStyle}" HorizontalAlignment="Stretch" Margin="0,0,0,6"/>
                <Button Content="🔼 Move Up" Command="{Binding MoveUpCommand}" CommandParameter="{Binding SelectedItem, ElementName=PdfListBox}" Style="{StaticResource ModernSecondaryButtonStyle}" HorizontalAlignment="Stretch" Margin="0,0,0,6"/>
                <Button Content="🔽 Move Down" Command="{Binding MoveDownCommand}" CommandParameter="{Binding SelectedItem, ElementName=PdfListBox}" Style="{StaticResource ModernSecondaryButtonStyle}" HorizontalAlignment="Stretch" Margin="0,0,0,6"/>
                <Button Content="❌ Remove" Command="{Binding RemoveFileCommand}" CommandParameter="{Binding SelectedItem, ElementName=PdfListBox}" Style="{StaticResource ModernSecondaryButtonStyle}" HorizontalAlignment="Stretch" Margin="0,0,0,6"/>
                <Button Content="🗑️ Clear All" Command="{Binding ClearAllCommand}" Style="{StaticResource ModernSecondaryButtonStyle}" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Grid>

        <!-- Save configurations parameters card -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Output filename row -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Filename: " FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding OutputFileName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" VerticalAlignment="Center"/>

                <!-- Output folder row -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Save to: " FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" VerticalAlignment="Center"/>
                <Button Grid.Row="2" Grid.Column="2" Content="Browse Folder" Command="{Binding BrowseOutputFolderCommand}" Style="{StaticResource ModernSecondaryButtonStyle}" VerticalAlignment="Center" Margin="8,0,0,0" Click="BrowseFolder_Click"/>
            </Grid>
        </Border>

        <!-- Actions panel (Merge, Cancel, and Progress feedback) -->
        <Border Grid.Row="3" Background="Transparent">
            <StackPanel>
                <Grid>
                    <!-- Progress Bar when merging -->
                    <ProgressBar Height="10" Minimum="0" Maximum="100" Value="{Binding ProgressValue}" Visibility="{Binding IsMerging, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,12"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Button Content="Merge PDF Files" Command="{Binding MergeCommand}" Style="{StaticResource ModernButtonStyle}" Width="160" Margin="0,0,8,0"/>
                        <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource ModernSecondaryButtonStyle}" Width="100" Visibility="{Binding IsMerging, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>

                    <!-- Status Label -->
                    <TextBlock Grid.Column="1" Text="{Binding MergeStatus}" FontSize="13" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="16,0" TextTrimming="CharacterEllipsis"/>

                    <!-- Page/File count status indicators -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Visibility="{Binding HasFiles, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="{Binding TotalPageCount, StringFormat={}{0} pages}" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,12,0"/>
                        <TextBlock Text="{Binding TotalFilesSizeFormatted, StringFormat={}Size: {0}}" FontSize="12" Foreground="{StaticResource MutedTextBrush}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
